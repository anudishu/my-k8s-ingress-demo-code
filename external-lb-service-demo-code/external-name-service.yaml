apiVersion: v1
kind: Service
metadata:
  name: externalsvc-dns
spec:
  type: ExternalName
  externalName:  cat-fact.herokuapp.com  ## put your external url or postgress url.
  #ports:
   # - port: 53
    #  protocol: UDP